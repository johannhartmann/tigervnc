version: 2
updates:
  # Keep the SHA-pinned third-party actions current. Grouped so a typical
  # minor/patch sweep produces one PR per week instead of one per action.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    open-pull-requests-limit: 5
    groups:
      actions:
        patterns: ["*"]
